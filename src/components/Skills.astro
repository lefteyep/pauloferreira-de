---
const skills = [
  'UX Design', 'UI Design', 'Product Design', 'Consulting',
  'Design Systems', 'Front-End Development', 'Workshops',
  'Design Sprint', 'Interaction Design', 'User Testing',
  'Usability Testing', 'UX Research', 'Leadership', 'Mentoring', 'No-Code',
];

const moreSkills = [
  'Figma', 'Prototyping', 'Wireframing', 'Branding',
  'Motion Design', 'Accessibility', 'Agile', 'Scrum',
];
---

<section id="skills" class="py-20 px-6 reveal" aria-label="Fähigkeiten">
  <div class="max-w-6xl mx-auto">
    <h2 class="text-3xl font-light text-[var(--color-text-muted)] mb-8">Skills</h2>
    <ul class="flex flex-wrap gap-3 list-none p-0" id="skills-list" role="list">
      {skills.map((skill, i) => (
        <li class="pill reveal" data-delay={String(i * 30)}>{skill}</li>
      ))}
      <li>
        <button
          id="show-more-skills"
          class="pill cursor-pointer hover:bg-white/5"
          aria-expanded="false"
          aria-controls="more-skills"
        >
          + Mehr anzeigen
        </button>
      </li>
    </ul>
    <ul id="more-skills" class="hidden flex-wrap gap-3 mt-3 list-none p-0" role="list">
      {moreSkills.map((skill, i) => (
        <li class="pill reveal" data-delay={String(i * 30)}>{skill}</li>
      ))}
    </ul>
  </div>
</section>

<script>
  const btn = document.getElementById('show-more-skills');
  const more = document.getElementById('more-skills');
  btn?.addEventListener('click', () => {
    const isHidden = more?.classList.contains('hidden');
    more?.classList.toggle('hidden');
    more?.classList.toggle('flex');
    if (btn) btn.textContent = isHidden ? '- Weniger' : '+ Mehr anzeigen';
    btn?.setAttribute('aria-expanded', isHidden ? 'true' : 'false');
    // Trigger reveal on newly visible items
    more?.querySelectorAll('.reveal').forEach(el => el.classList.add('visible'));
  });
</script>
